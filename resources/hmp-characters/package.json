{
  "name": "hmp-characters",
  "version": "0.2.0",
  "private": true,
  "author": "HogwartsMP",
  "description": "Character creation, selection and appearance flow for hmp-core.",
  "license": "SEE LICENSE IN LICENSE",
  "main": "dist/server.js",
  "scripts": {
    "build": "esbuild server/main.ts --bundle --platform=node --target=node22 --format=cjs --outfile=dist/server.js --legal-comments=inline && esbuild client/main.ts --bundle --platform=browser --target=es2022 --format=iife --outfile=dist/client.js --legal-comments=inline && node scripts/copy-static.mjs",
    "test": "tsx test/characters.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "smoke": "node test/bundle.test.js"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "typescript": "5.9.3"
  },
  "mafiahub": {
    "server": "dist/server.js",
    "client": "dist/client.js",
    "priority": -700,
    "errorBehavior": "stop",
    "exports": [
      "ui"
    ],
    "resourceDependencies": [
      { "name": "hmp-core", "version": "0.2.0" },
      { "name": "hmp-lib", "version": "0.2.0" }
    ]
  }
}
