// Resource dependency marker. The NPC API is server-only, but the Framework client must discover
// this resource so shared dependency validation can resolve hmp-gauntlet -> hmp-npcs.
console.log("[hmp-npcs] client dependency marker ready");
